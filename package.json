{
  "name": "context-lens",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that explains selected text based on what it actually is.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node test/pipeline.test.mjs && node test/stream.test.mjs && node test/messaging.test.mjs",
    "bench": "node bench/latency.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
